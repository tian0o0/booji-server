version: '2'

services:
  api:
    container_name: api
    build: booji-api
    ports:
      - 3333:3333
    #   depends_on:
    #     - admin
  
  admin:
    container_name: admin # default: booji-server-admin-1
    build: booji-admin # context
    ports:
      - 80:80
    restart: unless-stopped
  
